{
  "name": "pokrex",
  "version": "0.0.1",
  "license": "GPL-2.0",
  "private": true,
  "scripts": {
    "postinstall": "cd client && yarn install",
    "start": "echo 'visit http://localhost:3000/' && foreman start -f Procfile.dev",
    "test": "rspec"
  }
}
