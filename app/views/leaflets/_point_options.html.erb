<% scheme_type = room.scheme %>
  <ul data-scheme-type="<%= scheme_type.parameterize %>" class="<%= scheme_type.parameterize %> list-inline point-values" data-ticket-id="<%= story.id %>">
    <%= render_leaflet_option(room, story) %>
</ul>
