<% scheme_type = room.scheme %>
<ul data-scheme-type="<%= scheme_type.parameterize %>" class="<%= scheme_type.parameterize %> list-inline point-values <%= "active" if room.scheme == scheme_type %>">
  <%= render_point_options(Scheme.find_scheme(scheme_type)) %>
</ul>
